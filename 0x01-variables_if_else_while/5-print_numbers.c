#include <stdio.h>

/**
 * main - entry poing of program
 * Return: returns 0 on success
 */

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}

	printf("\n");
	return (0);
}

