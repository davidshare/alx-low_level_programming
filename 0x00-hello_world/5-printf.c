#include <stdio.h>

/**
 * main - outputs a string with a newline at the end using printf
 * Description: a program that outputs a string with a new line
 * character using printf
 * Return: 0 on success and non-zero number on failure
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

