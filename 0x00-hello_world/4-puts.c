#include <stdio.h>

/**
 * main - outputs a string with a newline at the end
 * Description: code that outputs a string with a new line character
 * Return: 0 on success and non-zero number on failure
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

